[project]
name = "crypto-dashboard"
version = "0.1.0"
description = ""
authors = [
    {name = "OfriRose",email = "ofri.rozner@gmail.com"}
]
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "requests (>=2.32.4,<3.0.0)",
    "pandas (>=2.3.1,<3.0.0)",
    "plotly (>=6.3.0,<7.0.0)",
    "streamlit (>=1.48.1,<2.0.0)"
]

[tool.poetry]
packages = [{include = "crypto_dashboard", from = "src"}]


[build-system]
requires = ["poetry-core>=2.0.0,<3.0.0"]
build-backend = "poetry.core.masonry.api"
